language: php

sudo: false

php:
  - 5.6
  - 7.0
  - 7.1
  - hhvm

before_script:
  - composer self-update
  - composer install --prefer-dist --no-interaction

script: phpunit -d memory_limit=1024M

matrix:
  fast_finish: true
